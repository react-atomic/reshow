{
  "description": "Reshow App Files Develop package",
  "repository": "react-atomic/reshow",
  "keywords": [
    "reactjs"
  ],
  "author": "Hill <hill@kimo.com>",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.x"
  },
  "scripts": {
    "clean": "find ./build -name '*.*' | xargs rm -rf",
    "copy": "cp package.json.build ./build/package.json && cp README.md ./build && cp .npmignore ./build && cp babelrc ./build/.babelrc",
    "build:babel": "BABEL_ENV=build babel src -d build --root-mode upward",
    "build": "npm run clean && npm run build:babel && npm run copy && cd build && yarn && npm i --package-lock-only",
    "prepublish": "npm run build"
  }
}
